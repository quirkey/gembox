#gems
  -@gems.sort.each do |gem_name, specs|
    .gem
      =link_to(gem_name, "/gems/#{gem_name}")
      .versions
        %span 
          Versions
          =specs.collect {|spec| spec.version }.join(', ')